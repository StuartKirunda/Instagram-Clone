import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    view: {
        width: '100%',
        height: 450,
    },
    image: {
        height: '100%',
        resizeMode: 'cover'
    },

});

export default styles;